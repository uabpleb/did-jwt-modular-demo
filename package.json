{
  "name": "did-jwt-passkey-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "POC: EdDSA + EIP-712 + WebAuthn VC-JWTs through one CompositeSigner/CompositeVerifier pair (index.html), plus the passkey deep-dive (passkey.html).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "@noble/curves": "^1.8.1",
    "@scure/base": "^2.2.0",
    "buffer": "^6.0.3",
    "did-jwt": "file:../did-jwt",
    "did-jwt-vc": "file:../did-jwt-vc",
    "did-jwt-webauthn-signer": "file:../did-jwt-webauthn-signer",
    "did-resolver": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^5.4.8"
  }
}
